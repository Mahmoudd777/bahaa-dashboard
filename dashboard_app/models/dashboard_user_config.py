# Deprecated: per-user dashboard configuration moved onto res.users
# (see models/res_users.py). This file is intentionally empty and no longer
# imported; the old dashboard.user.config model is removed on module upgrade.
